# 03 Data Raw

Purpose: immutable raw datasets and download/source notes.

Current status:
- Raw files for `credit_default` and `covertype` are present.
- Data provenance is recorded in `data_sources.md`.

Next action:
- Implement dataset loading and preprocessing in `../analysis/src/mia_eval/data.py`.
- Create cleaned data and reproducible splits in `../data_processed/`.
