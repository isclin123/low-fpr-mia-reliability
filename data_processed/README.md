# 04 Data Cleaned

Purpose: cleaned datasets, split files, data dictionaries, and preprocessing logs.

Current status:
- Cleaned datasets and reproducible member/non-member splits are present for `credit_default`, `covertype`, `adult_income`, `bank_marketing`, and `diabetes_readmission`.
- Each dataset has `cleaned_dataset.npz`, `member_nonmember_split.npz`, `metadata.json`, and `data_dictionary.md`.
- Shared preprocessing log: `preprocessing_log.md`.

Next action:
- Use these cleaned arrays and split records as the data source for final availability, reproducibility, and integrity checks.
